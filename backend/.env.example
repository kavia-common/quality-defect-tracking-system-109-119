# Backend environment variables (example)
#
# Canonical SQLite DB path for this multi-container repo:
# - Should point to the database container's DB file by default
# - Override in other environments as needed
SQLITE_DB=/home/kavia/workspace/code-generation/quality-defect-tracking-system-109-120/database/myapp.db
